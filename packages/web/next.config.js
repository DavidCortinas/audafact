/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Explicitly tell Next.js to only look in the root pages directory
  dir: ".",
  // Any other config options you need
};

module.exports = nextConfig;
